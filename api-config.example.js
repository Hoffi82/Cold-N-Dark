// Beispiel-Konfiguration für die spätere sichere Clash-of-Clans-API-Anbindung.
// NIEMALS einen echten API-Key in diese Datei oder in den öffentlichen Frontend-Code eintragen.

const COLD_N_DARK_API = {
  clanTag: '#C89CVRCP',
  endpoint: 'https://api.clashofclans.com/v1/clans/%23C89CVRCP/currentwar',
  status: 'backend-required'
};

// Der echte API-Key gehört ausschließlich auf einen Server/Backend-Dienst
// als Secret/Environment Variable, z. B. CLASH_API_TOKEN.
